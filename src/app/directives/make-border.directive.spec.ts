import { MakeBorderDirective } from './make-border.directive';

describe('MakeBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeBorderDirective();
    expect(directive).toBeTruthy();
  });
});
